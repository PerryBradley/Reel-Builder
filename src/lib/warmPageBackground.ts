import type { CSSProperties } from 'react'

/** Warm off-white → cream vertical gradient for builder shell pages. */
export const warmPageBackgroundStyle: CSSProperties = {
  background: 'linear-gradient(to bottom, #FAF9F7 0%, #F0EDE6 100%)',
}
